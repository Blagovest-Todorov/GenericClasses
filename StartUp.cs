using System;

namespace BoxOfT
{
    class StartUp
    {
        static void Main(string[] args)
        {
            //generic means General, common, not specific, not specific data type;
            //C#  allows you to define a generic classes, interfaces, abstract calsses, fields, methods, 
            //static methods, properties, , events, delgates, and operators using type parameter.
            // Type parameter <T> is a place holder for a particualr type specified when creating an insatnce
            // of the generic type; A generic type is decalred by specifying a type parameter in an angle brackets
            //after the type name e.g TypeName<T> where T is type parameter
            //Generic classes are defineded by using a type parameter in an angel brackets after the class name; 
            //class Fish<T>
            //making a class generic, lets you to reuse the code, type safety, and performance;
            //Advantiges of  Generics/ 1.Reasusability; 2.TypeSafety;3.BetterPerformance/
            //Generic type provide btter performance because they rduce the need of for boxing, unboxing, 
            //and typecasting of variables or objects.
        }
    }
}
